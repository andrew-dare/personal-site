name: Deploy production

on:
  workflow_dispatch: {}

permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-prod
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: prod
      url: ${{ vars.PROD_SITE_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build
        run: yarn build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.PROD_AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.PROD_AWS_REGION }}

      - name: Upload hashed assets (long cache)
        run: |
          aws s3 sync dist/assets "s3://${{ vars.PROD_S3_BUCKET_NAME }}/assets" \
            --delete \
            --cache-control "public,max-age=31536000,immutable"

      - name: Upload HTML and other files (short cache)
        run: |
          aws s3 sync dist "s3://${{ vars.PROD_S3_BUCKET_NAME }}" \
            --delete \
            --exclude "assets/*" \
            --cache-control "public,max-age=0,must-revalidate"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${{ vars.PROD_CLOUDFRONT_DISTRIBUTION_ID }}" \
            --paths "/*"
